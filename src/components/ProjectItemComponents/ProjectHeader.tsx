import { Typography } from 'antd';
const { Title } = Typography;

const ProjectHeader = () => {
    return <Title level={3} style={{ color: 'white', margin: 0 }}>Детали проекта</Title>;
};

export default ProjectHeader;
